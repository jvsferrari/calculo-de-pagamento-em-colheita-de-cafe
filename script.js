const table = [
    {namesColumn:"", costColumn:""}
]

const nameField = document.getElementById("name")
const pricePerField = document.getElementById("price");
const latao = document.getElementById("")

const nameTyped = nameField.value
const priceTyped = parseFloat(pricePerField.value)
const lataoTyped

table.push({
    namesColumn: name,
    costColumn: cost,
})